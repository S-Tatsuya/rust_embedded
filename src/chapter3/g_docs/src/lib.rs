/// Doubles the number given.
/// 
/// # Examples
/// 
/// ```
/// let result = docs::double(5);
/// println!("resutl = {}", result);
/// ```
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
